/**:
  ros__parameters:
    # Vehicle dimensions
    wheel_base: 2.39  # Distance between front and rear wheels in meters
    track_width: 1.34  # Distance between left and right wheels in meters
    wheel_radius: 0.4  # Wheel radius in meters
    # Safety limits
    max_steering_angle: 0.7  # Maximum steering angle in radians (about 40 degrees)
    max_wheel_speed: 10.0  # Maximum wheel angular velocity in rad/s
